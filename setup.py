from setuptools import setup, find_packages

setup(
    name="connect4",
    version="0.1",
    packages=find_packages(),
    description="A Connect 4 game with AI opponent",
    author="Tyler Vu",
    python_requires=">=3.9",
)